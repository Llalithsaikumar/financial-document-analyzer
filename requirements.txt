# Keep crewai at version 0.130.0
# Other package versions are flexible and can be changed
# Only change crewai version if there are critical dependency conflicts that cannot be resolved by other means
click==8.1.8

# Core framework
crewai==0.130.0
crewai-tools==0.47.1

# API / server
fastapi==0.110.3
uvicorn[standard]

# ML / data
numpy==1.26.4
pandas==2.2.2
onnxruntime==1.22.0

# Google / LLM access (let pip resolve compatible deps)
google-generativeai
google-api-python-client
google-cloud-storage
google-cloud-bigquery
google-cloud-aiplatform

# Validation / configs
pydantic>=2.4.2
python-dotenv

# Utilities
pillow
jinja2
jsonschema
protobuf>=4.25.3